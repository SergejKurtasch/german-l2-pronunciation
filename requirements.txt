# German Pronunciation Diagnostic App (L2-Trainer) - Requirements

# Core ML and Audio Processing
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# G2P (Grapheme-to-Phoneme)
phonemizer>=3.0.0

# Alignment
biopython>=1.81
textgrid>=1.5.0  # For parsing MFA TextGrid output files
ctc-forced-aligner>=0.1.0

# VAD (Voice Activity Detection)
silero-vad>=4.0.0
# Alternative: webrtcvad (optional, for fallback)

# ASR (Speech-to-Text)
openai-whisper>=20231117
# macOS native Speech framework (optional, for faster recognition on macOS)
pyobjc-framework-Speech>=9.0; sys_platform == "darwin"
pyobjc-framework-AVFoundation>=9.0; sys_platform == "darwin"
# OpenAI API client (for ASR_ENGINE="openai" — gpt-4o-transcribe; also used as openai-whisper dependency)
openai>=1.30.0
# Groq cloud ASR (optional alternative to OpenAI for ASR)
groq>=0.9.0

# Metrics
jiwer>=3.0.0

# Frontend (4.44+ recommended: Chatbot type="messages", stable api_info/schema with current stacks)
gradio>=4.44.1,<5.0.0

# Hugging Face Hub (transformers cache; optional second-stage validator model download)
# Upper bound: HfFolder removed in 0.37+, which breaks gradio 4.x oauth.py
huggingface_hub>=0.19.3,<0.37.0

# Utilities
pathlib2>=2.3.7; python_version < '3.4'

# Optional: second-stage phoneme validation (separate package; weights on Hugging Face, not in this repo)
#   pip install git+https://github.com/SergejKurtasch/german-phoneme-validator.git
# Models: https://huggingface.co/SergejKurt/german-phoneme-models (~1.74 GB on first download)
