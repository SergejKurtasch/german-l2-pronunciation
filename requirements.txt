# German Pronunciation Diagnostic App (L2-Trainer) - Requirements

# Core ML and Audio Processing
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# G2P (Grapheme-to-Phoneme)
phonemizer>=3.0.0

# Alignment
biopython>=1.81
textgrid>=1.5.0  # For parsing MFA TextGrid output files

# VAD (Voice Activity Detection)
silero-vad>=4.0.0
# Alternative: webrtcvad (optional, for fallback)

# ASR (Speech-to-Text)
openai-whisper>=20231117
# macOS native Speech framework (optional, for faster recognition on macOS)
pyobjc-framework-Speech>=9.0; sys_platform == "darwin"
pyobjc-framework-AVFoundation>=9.0; sys_platform == "darwin"

# Metrics
jiwer>=3.0.0

# Frontend
gradio>=4.0.0

# Utilities
pathlib2>=2.3.7; python_version < '3.4'

# Phoneme Validation (Second Stage Validation)
# Install from local directory (development):
#   pip install -e /path/to/german-phoneme-validator
# Or from GitHub:
#   pip install git+https://github.com/SergejKurtasch/german-phoneme-validator.git
# Or if published to PyPI:
#   german-phoneme-validator>=1.0.0
# Local development installation - use relative path or install from parent directory
# Example: pip install -e ../german-phoneme-validator
# Or install from GitHub: pip install git+https://github.com/SergejKurtasch/german-phoneme-validator.git


